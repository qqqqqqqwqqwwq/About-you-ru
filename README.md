# Распродажа — демо-витрина MVP

Мобильная витрина распродажи (женская / мужская одежда) на Next.js.  
Сайт называется **«Распродажа»** — это независимый демо/MVP, не связанный с брендом About You.

## Что умеет

- Разделы **Женщинам** / **Мужчинам**
- Карточки товаров из локального JSON (без авто-синхронизации каталога)
- Цены в **рублях**: `priceEur × курс ЦБ EUR/RUB × 1,5` (округление до целых ₽)
- Курс ЦБ: XML `https://www.cbr.ru/scripts/XML_daily.asp`, кэш ~24 ч; при ошибке — запасной курс
- Корзина в `localStorage` (размер, количество, удаление)
- Оформление заказа → `POST /api/order` → письмо через Resend (если настроены env), иначе soft-fail с логом
- **Нет** аккаунтов, скидок, авторизации

## Стек

- Next.js 14 (App Router) + TypeScript + Tailwind CSS
- Resend (опционально)

## Быстрый старт

```bash
npm install
cp .env.example .env.local   # при необходимости заполните ключи
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000).

Продакшен-сборка:

```bash
npm run build
npm start
```

## Переменные окружения

| Переменная | Обязательно | Описание |
|---|---|---|
| `RESEND_API_KEY` | нет | API-ключ Resend для писем о заказах |
| `ORDER_TO_EMAIL` | нет* | Email получателя заказов (*нужен вместе с ключом) |
| `ORDER_FROM_EMAIL` | нет | Отправитель (по умолчанию `onboarding@resend.dev`) |

Если ключи не заданы, заказ всё равно «принимается»: данные пишутся в лог сервера, UI показывает понятное сообщение.

## Структура

```
data/products.json     # образец каталога (16 SKU)
src/app/               # страницы и API
src/components/        # UI
src/lib/               # товары, ЦБ, цены, корзина
```

Страницы: `/`, `/women`, `/men`, `/product/[id]`, `/cart`, `/checkout`, `/checkout/success`.

## TODO (позже)

- [ ] Авто-обновление каталога по cron **08:00 / 15:00 / 22:00 (Москва)** — сейчас каталог только ручной JSON, без синка
- [ ] Реальная логистика и оплата
- [ ] Админка заказов

## Лицензия / дисклеймер

Демонстрационный MVP. Изображения — внешние CDN/Unsplash (placeholder при ошибке загрузки). Не используйте чужие товарные знаки в продакшене без прав.
